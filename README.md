To run it, create simple http server in the folder and run from localhost. 

Example of server in python 3.x: 
  `python -m http.server`
  
Example of server in python 2.x: 
  `python -m SimpleHTTPServer`
